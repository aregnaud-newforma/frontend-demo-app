## From the tests

AccountPage.integration.test.tsx:64 [Integration page] The account page shows a heading -> merged: AccountPage.integration.test.tsx "shows a loading state, then every stored field once the account arrives" (Reading your account - Default render)
AccountPage.integration.test.tsx:69 [Integration page] The account page's heading reads "Your account" -> merged: AccountPage.integration.test.tsx "shows a loading state, then every stored field once the account arrives" (Reading your account - Default render)
AccountPage.integration.test.tsx:74 [Integration page] The account page shows the account summary -> merged: AccountPage.integration.test.tsx "shows a loading state, then every stored field once the account arrives" (Reading your account - Default render)
AccountPage.integration.test.tsx:79 [Integration page] The account page shows a link to edit the account -> merged: AccountPage.integration.test.tsx "shows a loading state, then every stored field once the account arrives" (Reading your account - Default render)
AccountPage.integration.test.tsx:84 [Integration page] The edit link reads "Edit your account" -> merged: AccountPage.integration.test.tsx "shows a loading state, then every stored field once the account arrives" (Reading your account - Default render)
AccountPage.integration.test.tsx:93 [Integration page] The summary labels the name field "Name" -> merged: AccountPage.integration.test.tsx "shows a loading state, then every stored field once the account arrives" (Reading your account - Default render)
AccountPage.integration.test.tsx:98 [Integration page] The summary labels the first name field "First name" -> merged: AccountPage.integration.test.tsx "shows a loading state, then every stored field once the account arrives" (Reading your account - Default render)
AccountPage.integration.test.tsx:105 [Integration page] The summary labels the email field "Email" -> merged: AccountPage.integration.test.tsx "shows a loading state, then every stored field once the account arrives" (Reading your account - Default render)
AccountPage.integration.test.tsx:110 [Integration page] The summary labels the phone field "Phone" -> merged: AccountPage.integration.test.tsx "shows a loading state, then every stored field once the account arrives" (Reading your account - Default render)
AccountPage.integration.test.tsx:115 [Integration page] The summary labels the language field "Language" -> merged: AccountPage.integration.test.tsx "shows a loading state, then every stored field once the account arrives" (Reading your account - Default render)
AccountPage.integration.test.tsx:122 [Integration page] The summary labels the bio field "Bio" -> merged: AccountPage.integration.test.tsx "shows a loading state, then every stored field once the account arrives" (Reading your account - Default render)
AccountPage.integration.test.tsx:129 [Integration page] The summary shows the account's stored name -> merged: AccountPage.integration.test.tsx "shows a loading state, then every stored field once the account arrives" (Reading your account - Default render)
AccountPage.integration.test.tsx:134 [Integration page] The summary shows the account's stored first name -> merged: AccountPage.integration.test.tsx "shows a loading state, then every stored field once the account arrives" (Reading your account - Default render)
AccountPage.integration.test.tsx:141 [Integration page] The summary shows the account's stored email -> merged: AccountPage.integration.test.tsx "shows a loading state, then every stored field once the account arrives" (Reading your account - Default render)
AccountPage.integration.test.tsx:148 [Integration page] The summary shows the account's phone in national format -> merged: AccountPage.integration.test.tsx "shows a loading state, then every stored field once the account arrives" (Reading your account - Default render)
AccountPage.integration.test.tsx:155 [Integration page] The summary shows the account's language as its label, not the stored code -> merged: AccountPage.integration.test.tsx "shows a loading state, then every stored field once the account arrives" (Reading your account - Default render)
AccountPage.integration.test.tsx:162 [Integration page] The summary shows the account's stored bio -> merged: AccountPage.integration.test.tsx "shows a loading state, then every stored field once the account arrives" (Reading your account - Default render)
AccountPage.integration.test.tsx:167 [Integration page] The summary shows "Not provided" for the phone when the account has none -> merged: AccountPage.integration.test.tsx "shows \"Not provided\" for a missing phone and an empty bio, and every other field as stored" (Reading your account - Edge case)
AccountPage.integration.test.tsx:177 [Integration page] The summary shows "Not provided" for the bio when the account's bio is empty -> merged: AccountPage.integration.test.tsx "shows \"Not provided\" for a missing phone and an empty bio, and every other field as stored" (Reading your account - Edge case)
AccountPage.integration.test.tsx:189 [Integration page] The account page shows a loading indicator while the account is being fetched -> merged: AccountPage.integration.test.tsx "shows a loading state, then every stored field once the account arrives" (Reading your account - Default render)
AccountPage.integration.test.tsx:201 [Integration page] The loading indicator reads "Loading your account..." -> merged: AccountPage.integration.test.tsx "shows a loading state, then every stored field once the account arrives" (Reading your account - Default render)
AccountPage.integration.test.tsx:215 [Integration page] The loading indicator disappears once the account has loaded -> merged: AccountPage.integration.test.tsx "shows a loading state, then every stored field once the account arrives" (Reading your account - Default render)
AccountPage.integration.test.tsx:221 [Integration page] The account page shows an error indicator when the account fails to load -> merged: AccountPage.integration.test.tsx "shows an error and no summary when the account fails to load" (Reading your account - Edge case)
AccountPage.integration.test.tsx:228 [Integration page] The error indicator reads "Could not load your account. Please try again." -> merged: AccountPage.integration.test.tsx "shows an error and no summary when the account fails to load" (Reading your account - Edge case)
AccountPage.integration.test.tsx:237 [Integration page] The summary is not shown when the account fails to load -> merged: AccountPage.integration.test.tsx "shows an error and no summary when the account fails to load" (Reading your account - Edge case)
AccountPage.integration.test.tsx:247 [Integration page] Clicking the edit link takes the visitor away from the summary -> merged: AccountPage.integration.test.tsx "follows the edit link from the summary to the edit form" (Reading your account - Happy path)
AccountPage.integration.test.tsx:255 [Integration page] The account page loads the account data on render -> dropped: Cost without confidence - a spy on a collaborator; what the user sees proves the same wiring one level closer
AccountPage.integration.test.tsx:261 [Integration page] The account page loads the account without passing any options to the loader -> dropped: Cost without confidence - a spy on a collaborator; what the user sees proves the same wiring one level closer
AccountPage.integration.test.tsx:267 [Integration page] The account shown on the page comes from a request to the server -> dropped: Cost without confidence - a spy on a collaborator; what the user sees proves the same wiring one level closer
AccountPage.integration.test.tsx:273 [Integration page] The account received from the server is converted into display values before being shown -> dropped: Cost without confidence - a spy on a collaborator; what the user sees proves the same wiring one level closer
AccountPage.integration.test.tsx:279 [Integration page] The phone shown on the summary is the national-format value produced from the stored account -> dropped: Cost without confidence - a spy on a collaborator; what the user sees proves the same wiring one level closer
AccountPage.integration.test.tsx:287 [Integration page] The summary shows exactly one row for each of the six account fields: name, first name, email, phone, language, bio -> dropped: Cost without confidence - the props a child was handed, asserted through a spy; the rendered summary says the same thing
AccountPage.integration.test.tsx:296 [Integration page] The name row on the summary shows the account's stored name -> dropped: Cost without confidence - the props a child was handed, asserted through a spy; the rendered summary says the same thing
AccountPage.integration.test.tsx:305 [Integration page] The phone row on the summary shows "Not provided" when the account has no phone -> dropped: Cost without confidence - the props a child was handed, asserted through a spy; the rendered summary says the same thing
AccountPage.integration.test.tsx:317 [Integration page] No summary rows are shown while the account is still loading -> dropped: Cost without confidence - the props a child was handed, asserted through a spy; the rendered summary says the same thing
AccountPage.integration.test.tsx:327 [Integration page] The page shows a loading indicator whenever no account data is available yet -> dropped: Cost without confidence - useAccount is replaced, so this proves the stub
AccountPage.integration.test.tsx:334 [Integration page] The page shows an error indicator whenever the account failed to load -> dropped: Cost without confidence - useAccount is replaced, so this proves the stub
AccountPage.integration.test.tsx:341 [Integration page] If the account both has data and has failed, the page shows the error rather than the data -> dropped: Cost without confidence - useAccount is replaced, so this proves the stub; the behaviour stays open as blind spot 5
AccountPage.integration.test.tsx:348 [Integration page] The page can show an already-available account without making a network request -> dropped: Cost without confidence - useAccount is replaced, so this proves the stub
EditAccountPage.integration.test.tsx:59 [Integration page] The edit form is pre-filled with every stored field's current value -> merged: EditAccountPage.integration.test.tsx "shows a loading state, then every field seeded from the stored account" (Editing your account - Default render)
EditAccountPage.integration.test.tsx:78 [Integration page] The edit form shows a loading state until the account arrives -> merged: EditAccountPage.integration.test.tsx "shows a loading state, then every field seeded from the stored account" (Editing your account - Default render)
EditAccountPage.integration.test.tsx:98 [Integration page] The edit form shows an error, and no form fields, when the account cannot be loaded -> merged: EditAccountPage.integration.test.tsx "shows an error and no form fields when the account fails to load" (Editing your account - Edge case)
EditAccountPage.integration.test.tsx:110 [Integration page] Saving an edited account updates the changed fields, preserves the untouched ones, and returns the visitor to the summary showing the new value -> merged: EditAccountPage.integration.test.tsx "edits every field of an account with no phone, saves, and lands on the summary showing every new value" (Editing your account - Happy path)
EditAccountPage.integration.test.tsx:138 [Integration page] After saving, the summary shows the phone in national format, even when the account started with no phone -> merged: EditAccountPage.integration.test.tsx "edits every field of an account with no phone, saves, and lands on the summary showing every new value" (Editing your account - Happy path)
EditAccountPage.integration.test.tsx:153 [Integration page] Editing a field after a failed save clears the failure banner -> merged: EditAccountPage.integration.test.tsx "stays on the form and shows an error banner when the save fails, clearing it once the user edits again" (Editing your account - Edge case)
EditAccountPage.integration.test.tsx:169 [Integration page] Putting the language back to the placeholder option and saving is rejected with "Language is required" -> merged: EditAccountPage.integration.test.tsx "rejects the form when every field is invalid, flags each one, sends nothing, and clears a message the moment it is corrected" (Editing your account - Edge case)
EditAccountPage.integration.test.tsx:184 [Integration page] Clearing the name (a required field) and saving is rejected, shows "Name is required", and saves nothing -> merged: EditAccountPage.integration.test.tsx "rejects the form when every field is invalid, flags each one, sends nothing, and clears a message the moment it is corrected" (Editing your account - Edge case)
EditAccountPage.integration.test.tsx:201 [Integration page] Replacing the email with an invalid value is rejected, shows "Email is invalid", and saves nothing -> merged: EditAccountPage.integration.test.tsx "rejects the form when every field is invalid, flags each one, sends nothing, and clears a message the moment it is corrected" (Editing your account - Edge case)
EditAccountPage.integration.test.tsx:220 [Integration page] When the save request fails, the visitor stays on the form, sees "Something went wrong", and nothing is persisted -> merged: EditAccountPage.integration.test.tsx "stays on the form and shows an error banner when the save fails, clearing it once the user edits again" (Editing your account - Edge case)
EditAccountPage.integration.test.tsx:238 [Integration page] While a save is in flight, the Save button is disabled and relabelled "Saving...", then the visitor lands on the summary once it resolves -> merged: EditAccountPage.integration.test.tsx "edits every field of an account with no phone, saves, and lands on the summary showing every new value" (Editing your account - Happy path)
FieldError.integration.test.tsx:22 [Integration component] FieldError renders no error node when the field has no errors -> dropped: Cost without confidence - one page mounts FieldError, so its contract belongs inside that page's journey, where the flagged field, its message and its aria-invalid/aria-describedby pairing are asserted against the real form. The bare-string error shape (69) has no caller in this repo: every field is validated by the Zod schema
FieldError.integration.test.tsx:27 [Integration component] FieldError renders no error node for any field name when there are no errors -> dropped: Cost without confidence - one page mounts FieldError, so its contract belongs inside that page's journey, where the flagged field, its message and its aria-invalid/aria-describedby pairing are asserted against the real form. The bare-string error shape (69) has no caller in this repo: every field is validated by the Zod schema
FieldError.integration.test.tsx:34 [Integration component] FieldError renders an error node when the field is invalid -> dropped: Cost without confidence - one page mounts FieldError, so its contract belongs inside that page's journey, where the flagged field, its message and its aria-invalid/aria-describedby pairing are asserted against the real form. The bare-string error shape (69) has no caller in this repo: every field is validated by the Zod schema
FieldError.integration.test.tsx:41 [Integration component] FieldError shows the error message inside the error node -> dropped: Cost without confidence - one page mounts FieldError, so its contract belongs inside that page's journey, where the flagged field, its message and its aria-invalid/aria-describedby pairing are asserted against the real form. The bare-string error shape (69) has no caller in this repo: every field is validated by the Zod schema
FieldError.integration.test.tsx:48 [Integration component] FieldError names the error node after the field it belongs to -> dropped: Cost without confidence - one page mounts FieldError, so its contract belongs inside that page's journey, where the flagged field, its message and its aria-invalid/aria-describedby pairing are asserted against the real form. The bare-string error shape (69) has no caller in this repo: every field is validated by the Zod schema
FieldError.integration.test.tsx:55 [Integration component] FieldError's error node carries an id built from the field name (e.g. "email-error") -> dropped: Cost without confidence - one page mounts FieldError, so its contract belongs inside that page's journey, where the flagged field, its message and its aria-invalid/aria-describedby pairing are asserted against the real form. The bare-string error shape (69) has no caller in this repo: every field is validated by the Zod schema
FieldError.integration.test.tsx:62 [Integration component] FieldError builds the error node's id from whichever field name it is given (e.g. "nom-error") -> dropped: Cost without confidence - one page mounts FieldError, so its contract belongs inside that page's journey, where the flagged field, its message and its aria-invalid/aria-describedby pairing are asserted against the real form. The bare-string error shape (69) has no caller in this repo: every field is validated by the Zod schema
FieldError.integration.test.tsx:69 [Integration component] FieldError renders a bare string error message -> dropped: Cost without confidence - one page mounts FieldError, so its contract belongs inside that page's journey, where the flagged field, its message and its aria-invalid/aria-describedby pairing are asserted against the real form. The bare-string error shape (69) has no caller in this repo: every field is validated by the Zod schema
FieldError.integration.test.tsx:74 [Integration component] FieldError renders a Standard Schema issue error message -> dropped: Cost without confidence - one page mounts FieldError, so its contract belongs inside that page's journey, where the flagged field, its message and its aria-invalid/aria-describedby pairing are asserted against the real form. The bare-string error shape (69) has no caller in this repo: every field is validated by the Zod schema
FieldError.integration.test.tsx:81 [Integration component] FieldError shows only the first of several errors on the field -> dropped: Cost without confidence - one page mounts FieldError, so its contract belongs inside that page's journey, where the flagged field, its message and its aria-invalid/aria-describedby pairing are asserted against the real form. The bare-string error shape (69) has no caller in this repo: every field is validated by the Zod schema
FieldError.integration.test.tsx:89 [Integration component] FieldError renders exactly one error node even when there are several errors -> dropped: Cost without confidence - one page mounts FieldError, so its contract belongs inside that page's journey, where the flagged field, its message and its aria-invalid/aria-describedby pairing are asserted against the real form. The bare-string error shape (69) has no caller in this repo: every field is validated by the Zod schema
FieldError.integration.test.tsx:100 [Integration component] errorProps returns an empty object for a valid field -> dropped: Cost without confidence - one page mounts FieldError, so its contract belongs inside that page's journey, where the flagged field, its message and its aria-invalid/aria-describedby pairing are asserted against the real form. The bare-string error shape (69) has no caller in this repo: every field is validated by the Zod schema
FieldError.integration.test.tsx:104 [Integration component] errorProps does not set aria-invalid on a valid field -> dropped: Cost without confidence - one page mounts FieldError, so its contract belongs inside that page's journey, where the flagged field, its message and its aria-invalid/aria-describedby pairing are asserted against the real form. The bare-string error shape (69) has no caller in this repo: every field is validated by the Zod schema
FieldError.integration.test.tsx:108 [Integration component] errorProps sets aria-invalid on an invalid field -> dropped: Cost without confidence - one page mounts FieldError, so its contract belongs inside that page's journey, where the flagged field, its message and its aria-invalid/aria-describedby pairing are asserted against the real form. The bare-string error shape (69) has no caller in this repo: every field is validated by the Zod schema
FieldError.integration.test.tsx:112 [Integration component] errorProps sets aria-describedby on an invalid field -> dropped: Cost without confidence - one page mounts FieldError, so its contract belongs inside that page's journey, where the flagged field, its message and its aria-invalid/aria-describedby pairing are asserted against the real form. The bare-string error shape (69) has no caller in this repo: every field is validated by the Zod schema
FieldError.integration.test.tsx:119 [Integration component] errorProps' aria-describedby points at the id the rendered error node actually carries -> dropped: Cost without confidence - one page mounts FieldError, so its contract belongs inside that page's journey, where the flagged field, its message and its aria-invalid/aria-describedby pairing are asserted against the real form. The bare-string error shape (69) has no caller in this repo: every field is validated by the Zod schema
SummaryRow.integration.test.tsx:19 [Integration component] SummaryRow renders without crashing -> dropped: Cost without confidence - one page mounts SummaryRow, and the account summary now asserts each term against its value
SummaryRow.integration.test.tsx:24 [Integration component] SummaryRow renders the term -> dropped: Cost without confidence - one page mounts SummaryRow, and the account summary now asserts each term against its value
SummaryRow.integration.test.tsx:29 [Integration component] SummaryRow renders the value -> dropped: Cost without confidence - one page mounts SummaryRow, and the account summary now asserts each term against its value
SummaryRow.integration.test.tsx:34 [Integration component] SummaryRow renders the term inside a dt -> dropped: Cost without confidence - one page mounts SummaryRow, and the account summary now asserts each term against its value
SummaryRow.integration.test.tsx:40 [Integration component] SummaryRow renders the value inside a dd -> dropped: Cost without confidence - one page mounts SummaryRow, and the account summary now asserts each term against its value
SummaryRow.integration.test.tsx:46 [Integration component] SummaryRow renders the term before the value -> dropped: Cost without confidence - one page mounts SummaryRow, and the account summary now asserts each term against its value
SummaryRow.integration.test.tsx:53 [Integration component] SummaryRow renders exactly one term/value row -> dropped: Cost without confidence - one page mounts SummaryRow, and the account summary now asserts each term against its value
SummaryRow.integration.test.tsx:62 [Integration component] SummaryRow accepts a plain string as its value -> dropped: Cost without confidence - one page mounts SummaryRow, and the account summary now asserts each term against its value
SummaryRow.integration.test.tsx:67 [Integration component] SummaryRow accepts a number as its value -> dropped: Cost without confidence - one page mounts SummaryRow, and the account summary now asserts each term against its value
SummaryRow.integration.test.tsx:72 [Integration component] SummaryRow accepts an element, such as a link, as its value -> dropped: Cost without confidence - one page mounts SummaryRow, and the account summary now asserts each term against its value
SummaryRow.integration.test.tsx:77 [Integration component] SummaryRow renders an empty value when given an empty string -> dropped: Cost without confidence - one page mounts SummaryRow, and the account summary now asserts each term against its value
SummaryRow.integration.test.tsx:83 [Integration component] SummaryRow can show the "Not provided" fallback text passed to it -> dropped: Cost without confidence - one page mounts SummaryRow, and the account summary now asserts each term against its value
SummaryRow.integration.test.tsx:90 [Integration component] SummaryRow renders the "Name" term used by the account summary -> dropped: Cost without confidence - one page mounts SummaryRow, and the account summary now asserts each term against its value
SummaryRow.integration.test.tsx:95 [Integration component] SummaryRow renders the "First name" term used by the account summary -> dropped: Cost without confidence - one page mounts SummaryRow, and the account summary now asserts each term against its value
SummaryRow.integration.test.tsx:100 [Integration component] SummaryRow renders the "Phone" term used by the account summary -> dropped: Cost without confidence - one page mounts SummaryRow, and the account summary now asserts each term against its value
SummaryRow.integration.test.tsx:105 [Integration component] SummaryRow renders the "Language" term used by the account summary -> dropped: Cost without confidence - one page mounts SummaryRow, and the account summary now asserts each term against its value
SummaryRow.integration.test.tsx:110 [Integration component] SummaryRow renders the "Bio" term used by the account summary -> dropped: Cost without confidence - one page mounts SummaryRow, and the account summary now asserts each term against its value
api.unit.test.ts:29 [Unit] `toValues` returns an object -> dropped: Static already holds it - the field-for-field copy is what AccountValues and AccountPayload declare, and the compiler checks it
api.unit.test.ts:33 [Unit] `toValues` copies `nom` from the account into the values -> dropped: Static already holds it - the field-for-field copy is what AccountValues and AccountPayload declare, and the compiler checks it
api.unit.test.ts:37 [Unit] `toValues` copies `prenom` -> dropped: Static already holds it - the field-for-field copy is what AccountValues and AccountPayload declare, and the compiler checks it
api.unit.test.ts:41 [Unit] `toValues` copies `email` -> dropped: Static already holds it - the field-for-field copy is what AccountValues and AccountPayload declare, and the compiler checks it
api.unit.test.ts:45 [Unit] `toValues` copies `langue` -> dropped: Static already holds it - the field-for-field copy is what AccountValues and AccountPayload declare, and the compiler checks it
api.unit.test.ts:49 [Unit] `toValues` copies `bio` -> dropped: Static already holds it - the field-for-field copy is what AccountValues and AccountPayload declare, and the compiler checks it
api.unit.test.ts:53 [Unit] `toValues` does not carry `id` into the returned values -> dropped: Static already holds it - the field-for-field copy is what AccountValues and AccountPayload declare, and the compiler checks it
api.unit.test.ts:57 [Unit] `toValues` converts a stored E.164 phone to national format -> merged: api.unit.test.ts, the single it.each over the phone conversions toValues and toPayload disagree on
api.unit.test.ts:61 [Unit] `toValues` returns an empty string for `telephone` when the account's phone is `null` -> merged: api.unit.test.ts, the single it.each over the phone conversions toValues and toPayload disagree on
api.unit.test.ts:65 [Unit] `toValues` returns an empty string for `telephone` when the stored value cannot be parsed -> merged: api.unit.test.ts, the single it.each over the phone conversions toValues and toPayload disagree on
api.unit.test.ts:71 [Unit] `toPayload` returns an object -> dropped: Static already holds it - the field-for-field copy is what AccountValues and AccountPayload declare, and the compiler checks it
api.unit.test.ts:75 [Unit] `toPayload` copies `nom` from the values into the payload -> dropped: Static already holds it - the field-for-field copy is what AccountValues and AccountPayload declare, and the compiler checks it
api.unit.test.ts:79 [Unit] `toPayload` copies `prenom` -> dropped: Static already holds it - the field-for-field copy is what AccountValues and AccountPayload declare, and the compiler checks it
api.unit.test.ts:83 [Unit] `toPayload` copies `email` -> dropped: Static already holds it - the field-for-field copy is what AccountValues and AccountPayload declare, and the compiler checks it
api.unit.test.ts:87 [Unit] `toPayload` copies `langue` -> dropped: Static already holds it - the field-for-field copy is what AccountValues and AccountPayload declare, and the compiler checks it
api.unit.test.ts:91 [Unit] `toPayload` copies `bio` -> dropped: Static already holds it - the field-for-field copy is what AccountValues and AccountPayload declare, and the compiler checks it
api.unit.test.ts:95 [Unit] `toPayload` does not add an `id` to the payload -> dropped: Static already holds it - the field-for-field copy is what AccountValues and AccountPayload declare, and the compiler checks it
api.unit.test.ts:99 [Unit] `toPayload` normalizes a national phone to E.164 -> merged: api.unit.test.ts, the single it.each over the phone conversions toValues and toPayload disagree on
api.unit.test.ts:103 [Unit] `toPayload` sends `null` for `telephone` when the phone value is an empty string -> merged: api.unit.test.ts, the single it.each over the phone conversions toValues and toPayload disagree on
api.unit.test.ts:107 [Unit] `toValues` and `toPayload` round-trip `nom`, `prenom`, and `email` unchanged -> dropped: Static already holds it - the field-for-field copy is what AccountValues and AccountPayload declare, and the compiler checks it
phone.unit.test.ts:10 [Unit] `parseFrenchPhone` normalizes a national number with spaces ("06 12 34 56 78") to e164 "+33612345678" / national "0612345678" -> merged: phone.unit.test.ts, the single it.each over every accepted shape and rejection reason
phone.unit.test.ts:11 [Unit] `parseFrenchPhone` normalizes a national number with dots ("06.12.34.56.78") to the same canonical form -> merged: phone.unit.test.ts, the single it.each over every accepted shape and rejection reason
phone.unit.test.ts:12 [Unit] `parseFrenchPhone` normalizes a national number with dashes ("06-12-34-56-78") to the same canonical form -> merged: phone.unit.test.ts, the single it.each over every accepted shape and rejection reason
phone.unit.test.ts:13 [Unit] `parseFrenchPhone` normalizes a plain national number ("0612345678") to the same canonical form -> merged: phone.unit.test.ts, the single it.each over every accepted shape and rejection reason
phone.unit.test.ts:14 [Unit] `parseFrenchPhone` normalizes an international +33 number ("+33 6 12 34 56 78") to the same canonical form -> merged: phone.unit.test.ts, the single it.each over every accepted shape and rejection reason
phone.unit.test.ts:15 [Unit] `parseFrenchPhone` normalizes an international 0033 number ("0033612345678") to the same canonical form -> merged: phone.unit.test.ts, the single it.each over every accepted shape and rejection reason
phone.unit.test.ts:26 [Unit] `parseFrenchPhone` rejects an empty string with reason "empty" -> merged: phone.unit.test.ts, the single it.each over every accepted shape and rejection reason
phone.unit.test.ts:27 [Unit] `parseFrenchPhone` rejects whitespace-only input with reason "empty" -> merged: phone.unit.test.ts, the single it.each over every accepted shape and rejection reason
phone.unit.test.ts:28 [Unit] `parseFrenchPhone` rejects letters in the number with reason "invalid_chars" -> merged: phone.unit.test.ts, the single it.each over every accepted shape and rejection reason
phone.unit.test.ts:29 [Unit] `parseFrenchPhone` rejects a number that is too short with reason "wrong_length" -> merged: phone.unit.test.ts, the single it.each over every accepted shape and rejection reason
phone.unit.test.ts:30 [Unit] `parseFrenchPhone` rejects a number that is too long with reason "wrong_length" -> merged: phone.unit.test.ts, the single it.each over every accepted shape and rejection reason
phone.unit.test.ts:31 [Unit] `parseFrenchPhone` rejects a number missing its leading zero with reason "invalid_prefix" -> merged: phone.unit.test.ts, the single it.each over every accepted shape and rejection reason
phone.unit.test.ts:32 [Unit] `parseFrenchPhone` rejects a number with an invalid group digit "0" with reason "invalid_prefix" -> merged: phone.unit.test.ts, the single it.each over every accepted shape and rejection reason
phone.unit.test.ts:37 [Unit] `parseFrenchPhone` accepts a landline number (group digit 1) -> merged: phone.unit.test.ts, the single it.each over every accepted shape and rejection reason
phone.unit.test.ts:46 [Unit] `isValidFrenchPhone` returns true for a valid number and false for an invalid one -> merged: phone.unit.test.ts, the single it.each over every accepted shape and rejection reason
validation.unit.test.ts:23 [Unit] `accountSchema` is defined -> dropped: Not this repo's behaviour - that is Zod's API
validation.unit.test.ts:27 [Unit] `accountSchema` exposes a `safeParse` method -> dropped: Not this repo's behaviour - that is Zod's API
validation.unit.test.ts:31 [Unit] `accountSchema.safeParse` returns a result object with a `success` flag -> dropped: Not this repo's behaviour - that is Zod's API
validation.unit.test.ts:36 [Unit] `accountSchema` accepts a fully valid account -> merged: validation.unit.test.ts accountSchema acceptance table - proved by the base `valid` fixture every row parses (Editing your account)
validation.unit.test.ts:42 [Unit] `accountSchema` rejects an empty `nom` -> merged: validation.unit.test.ts accountSchema rejection table (Editing your account)
validation.unit.test.ts:47 [Unit] `accountSchema`'s message for an empty `nom` is "Name is required" -> merged: validation.unit.test.ts accountSchema rejection table (Editing your account)
validation.unit.test.ts:53 [Unit] `accountSchema` rejects a whitespace-only `nom` -> merged: validation.unit.test.ts accountSchema rejection table (Editing your account)
validation.unit.test.ts:60 [Unit] `accountSchema` rejects an empty `prenom` -> merged: validation.unit.test.ts accountSchema rejection table (Editing your account)
validation.unit.test.ts:65 [Unit] `accountSchema`'s message for an empty `prenom` is "First name is required" -> merged: validation.unit.test.ts accountSchema rejection table (Editing your account)
validation.unit.test.ts:73 [Unit] `accountSchema` rejects an empty `email` -> merged: validation.unit.test.ts accountSchema rejection table (Editing your account)
validation.unit.test.ts:78 [Unit] `accountSchema`'s message for an empty `email` is "Email is required" -> merged: validation.unit.test.ts accountSchema rejection table (Editing your account)
validation.unit.test.ts:84 [Unit] `accountSchema` rejects an email with no "@" -> merged: validation.unit.test.ts accountSchema rejection table (Editing your account)
validation.unit.test.ts:89 [Unit] `accountSchema` rejects an email with no domain -> merged: validation.unit.test.ts accountSchema rejection table (Editing your account)
validation.unit.test.ts:94 [Unit] `accountSchema`'s message for a malformed email is "Email is invalid" -> merged: validation.unit.test.ts accountSchema rejection table (Editing your account)
validation.unit.test.ts:100 [Unit] `accountSchema` accepts a normal email -> merged: validation.unit.test.ts accountSchema acceptance table - proved by the base `valid` fixture every row parses (Editing your account)
validation.unit.test.ts:107 [Unit] `accountSchema` accepts an empty `telephone` -> merged: validation.unit.test.ts accountSchema acceptance table (Editing your account)
validation.unit.test.ts:112 [Unit] `accountSchema` accepts a national-format `telephone` -> merged: validation.unit.test.ts accountSchema acceptance table - proved by the base `valid` fixture every row parses (Editing your account)
validation.unit.test.ts:117 [Unit] `accountSchema` accepts an international-format `telephone` -> OPEN - the approved plan neither merged nor dropped this line: the acceptance table's fixture is national-format and "fr", so an international telephone and langue "en" are still unproved
validation.unit.test.ts:122 [Unit] `accountSchema` rejects a `telephone` containing letters -> merged: validation.unit.test.ts accountSchema rejection table (Editing your account)
validation.unit.test.ts:127 [Unit] `accountSchema` rejects a `telephone` that is too short -> merged: validation.unit.test.ts accountSchema rejection table (Editing your account)
validation.unit.test.ts:134 [Unit] `accountSchema` accepts `langue` "fr" -> merged: validation.unit.test.ts accountSchema acceptance table - proved by the base `valid` fixture every row parses (Editing your account)
validation.unit.test.ts:139 [Unit] `accountSchema` accepts `langue` "en" -> OPEN - the approved plan neither merged nor dropped this line: the acceptance table's fixture is national-format and "fr", so an international telephone and langue "en" are still unproved
validation.unit.test.ts:144 [Unit] `accountSchema` rejects the empty placeholder `langue` option -> merged: validation.unit.test.ts accountSchema rejection table (Editing your account)
validation.unit.test.ts:149 [Unit] `accountSchema`'s message for the placeholder `langue` is "Language is required" -> merged: validation.unit.test.ts accountSchema rejection table (Editing your account)
validation.unit.test.ts:157 [Unit] `accountSchema` accepts an empty `bio` -> merged: validation.unit.test.ts accountSchema acceptance table (Editing your account)
validation.unit.test.ts:162 [Unit] `accountSchema` accepts a `bio` of exactly 200 characters -> merged: validation.unit.test.ts accountSchema acceptance table (Editing your account)
validation.unit.test.ts:167 [Unit] `accountSchema` rejects a `bio` of 201 characters -> merged: validation.unit.test.ts accountSchema rejection table (Editing your account)
validation.unit.test.ts:175 [Unit] `LANGUAGES` contains "fr" -> dropped: Static already holds it - LANGUAGES is an `as const` tuple and LANGUAGE_LABELS is a Record<Language, string>
validation.unit.test.ts:179 [Unit] `LANGUAGES` contains "en" -> dropped: Static already holds it - LANGUAGES is an `as const` tuple and LANGUAGE_LABELS is a Record<Language, string>
validation.unit.test.ts:183 [Unit] `LANGUAGES` holds exactly two languages -> dropped: Static already holds it - LANGUAGES is an `as const` tuple and LANGUAGE_LABELS is a Record<Language, string>
validation.unit.test.ts:189 [Unit] `LANGUAGE_LABELS` labels "fr" as "French" -> dropped: Pins wording, not behaviour - the labels reaching the screen instead of the stored codes is asserted where a user reads them, on the summary and in the select
validation.unit.test.ts:193 [Unit] `LANGUAGE_LABELS` labels "en" as "English" -> dropped: Pins wording, not behaviour - the labels reaching the screen instead of the stored codes is asserted where a user reads them, on the summary and in the select
validation.unit.test.ts:197 [Unit] `LANGUAGE_LABELS` has a label for every language in `LANGUAGES` -> dropped: Static already holds it - LANGUAGES is an `as const` tuple and LANGUAGE_LABELS is a Record<Language, string>
Navigation.integration.test.tsx:46 [Integration component] Following the account link in the nav takes the visitor from the welcome page to their account -> merged: Navigation.integration.test.tsx "marks the current route as the visitor moves from the welcome to the account and the edit form, and Home always returns them to the welcome" (Navigation - Happy path)
Navigation.integration.test.tsx:58 [Integration component] Following the home link in the nav takes the visitor back to the welcome page from any other page, e.g. the edit form -> merged: Navigation.integration.test.tsx "marks the current route as the visitor moves from the welcome to the account and the edit form, and Home always returns them to the welcome" (Navigation - Happy path)
Navigation.integration.test.tsx:71 [Integration component] The nav marks only the page the visitor is currently on as current (aria-current="page"), and the mark moves with them as they navigate -> merged: Navigation.integration.test.tsx "marks the current route as the visitor moves from the welcome to the account and the edit form, and Home always returns them to the welcome" (Navigation - Happy path)
account.spec.ts:24 [E2E] A visitor can go from the welcome page to their account, see every stored field on the summary, edit every field on the edit form, save, and land back on the summary with every field showing its new value -> merged: e2e/account.spec.ts "visitor navigates to their account, edits every field and is returned to the summary" (Happy path) - written, not run: the e2e chain needs a build and two servers

## From the code

validation.ts:23 The edit form's telephone field has its own validation message ("Phone number is invalid") when the number entered is invalid, but no test ever provokes it - only that an invalid phone is rejected, never what it says -> blind spot closed: EditAccountPage.integration.test.tsx "rejects the form when every field is invalid, flags each one, sends nothing, and clears a message the moment it is corrected" (Editing your account - Edge case)
EditAccountPage.tsx:137 The "Cancel" link on the edit form, which takes the visitor back to the summary without saving, is never clicked or even located by any test -> blind spot closed: EditAccountPage.integration.test.tsx "abandons the edit: Cancel returns to the summary leaving the stored account unchanged" (Editing your account - Edge case)
EditAccountPage.tsx:160 Correcting an invalid field after a rejected save re-validates it live (the form's `revalidateLogic`), clearing its error without another Save click - no test edits a field after a failed submission to check the error goes away on its own -> blind spot closed: EditAccountPage.integration.test.tsx "rejects the form when every field is invalid, flags each one, sends nothing, and clears a message the moment it is corrected" (Editing your account - Edge case)
server/api.ts:78 A request that names no session (the state a visitor is in when they open the app with `yarn dev` and never went through the E2E seeding flow) is served the shared "demo" account seeded at server startup, rather than an error - no test ever omits the session cookie
EditAccountPage.tsx:117 On the edit form, an account that has both loaded data and a load failure shows the error rather than the stale form (same branch order as AccountPage) - AccountPage proves this priority, EditAccountPage's own copy of it does not -> left open - blind spot 5: reaching it needs a successful load followed by a failed refetch, and neither page ever refetches
EditAccountPage.tsx:283 The language select's placeholder option reads "Choose a language" - only that leaving it selected is rejected is asserted, never the option's own text -> blind spot closed: EditAccountPage.integration.test.tsx "shows a loading state, then every field seeded from the stored account" (Editing your account - Default render)

## Census

```
e2e/account.spec.ts
server/api.ts
src/account/AccountPage.tsx
src/account/EditAccountPage.tsx
src/account/__tests__/AccountPage.integration.test.tsx
src/account/__tests__/EditAccountPage.integration.test.tsx
src/account/components/FieldError.tsx
src/account/components/SummaryRow.tsx
src/account/components/__tests__/FieldError.integration.test.tsx
src/account/components/__tests__/SummaryRow.integration.test.tsx
src/account/helpers/__tests__/api.unit.test.ts
src/account/helpers/__tests__/phone.unit.test.ts
src/account/helpers/__tests__/validation.unit.test.ts
src/account/helpers/api.ts
src/account/helpers/phone.ts
src/account/helpers/validation.ts
src/account/hooks/use-account.ts
src/account/mocks/db-utils.ts
src/account/mocks/db.ts
src/account/mocks/email-factory.ts
src/account/mocks/handlers.ts
src/layout/Navigation.tsx
src/layout/RootLayout.tsx
src/layout/__tests__/Navigation.integration.test.tsx
src/query-client.ts
src/routes.tsx
```
