| Where                   | When                              | What                                              | Arms                                                                                                                                                                                                                                                                                                   | Question                                                                            |
| ----------------------- | --------------------------------- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------- |
| **Local**               | while writing a rule or a grader  | 1 task, `--reuse`, `repeat 1`                     | `with-skills`; add `previous-skill --baseline-ref HEAD~1` once the rule edit is committed, to see whether the edit moved anything; add `without-skills` once, when the task is new, to prove the prompt can tell the two apart                                                                         | "Does my grader score right? Does my rule change anything? Did my edit to it help?" |
| **CI on PR**            | every push that touches a rule    | targeted subset, code graders preferred, fast     | `with-skills` + `previous-skill` at the merge base, for the gates whose skill the PR touched — same CLI, model and hour, so the delta is the edit's effect; add `without-skills` only when the PR touches the prompts (`evals/`), `AGENTS.md` or the pinned model/CLI, where the ablation itself moved | "Did I break something obvious? Is the edit why the score moved?"                   |
| **Scheduled (nightly)** | every night or every week         | full suite, LLM judges, high `repeat`             | `with-skills` + `without-skills` — the gap between them is the skill's worth, and a gap closing over time means the model learned the rule and the skill is decoration. Never `previous-skill`: `main` against `main` is refused                                                                       | "What is the skill really worth, and is it drifting?"                               |
| **Online (every PR)**   | every pull request of an app repo | the real diff, `online` graders only, report-only | none: no exercise and no ablation, the diff is whatever the author wanted. `onlineJudge: false` in `evals.config.ts` until the online criteria have been read against real diffs                                                                                                                       | "Do the agents people actually use follow the rule, on real work?"                  |
